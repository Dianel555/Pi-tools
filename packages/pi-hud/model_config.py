"""Model compatibility metadata kept separate from UI themes."""

# Runtime/provider names can differ from the model names in models.json.
KNOWN_CONTEXT_WINDOWS = {
    ("duckcode", "gpt-5.6-luna-max"): 272000,
    ("duckcode", "gpt-5.6-terra-ultra"): 272000,
}
